# 云窑系统 - 数据库说明文档

> 数据库：`yunyao`（MySQL）
> 编码：utf8mb4

---

## 表目录

| # | 表名 | 所属模块 | 简要说明 |
|---|------|----------|----------|
| 1 | `users` | 用户与权限 | 用户账号、登录信息、等级 |
| 2 | `user_levels` | 用户与权限 | 用户等级定义及发布权限 |
| 3 | `user_settings` | 用户与权限 | 用户默认材料/窑炉/温度偏好 |
| 4 | `recipes` | 配方系统 | 配方主表：标题、描述、分类、釉色 |
| 5 | `recipe_ingredients` | 配方系统 | 配方配料明细：原料、用量、排序 |
| 6 | `recipe_views` | 配方系统 | 配方浏览记录 |
| 7 | `recipe_sequences` | 配方系统 | 配方编号生成器（字母+计数器） |
| 8 | `works` | 作品系统 | 烧制作品：图片、描述、釉色、属性 |
| 9 | `work_comments` | 作品系统 | 作品评论（支持嵌套） |
| 10 | `work_attribute_options` | 作品系统 | 作品可选属性配置（窑/温度/釉面等） |
| 12 | `reviews` | 评价 | 评价及回复（含烧制参数） |
| 13 | `favorites` | 社交互动 | 用户收藏（配方/作品） |
| 14 | `likes` | 社交互动 | 用户点赞（配方/作品） |
| 15 | `follows` | 社交互动 | 用户关注关系 |
| 16 | `messages` | 社交互动 | 站内私信 |
| 17 | `notifications` | 社交互动 | 系统通知（评论/关注/点赞/收藏） |
| 18 | `complaints` | 社交互动 | 用户投诉/反馈 |
| 19 | `user_materials` | 材料库存 | 用户材料库存/待购清单 |
| 20 | `materials` | 釉料知识 | 合并后的统一原材料目录及氧化物数据 |
| 21 | `glossary_terms` | 釉料知识 | 陶瓷术语定义 |
| 22 | `temperature_cones` | 釉料知识 | 奥尔顿测温锥温度对照 |
| 23 | `firing_curves` | 釉料知识 | 烧制曲线模板（多段升温） |
| 24 | `body_materials` | 釉料知识 | 坯体料类型 |
| 26 | `app_settings` | 系统配置 | 应用配置键值（SMTP/搜索范围等） |
| 28 | `to_be_fired` | 待烧队列 | 用户待烧配方队列（备忘/状态） |

---|---

## 一、用户与权限

### 1. `users` 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 用户ID |
| username | VARCHAR(100) UNIQUE | 用户名 |
| email | VARCHAR(100) UNIQUE | 邮箱 |
| phone | VARCHAR(20) UNIQUE | 手机号 |
| password_hash | VARCHAR(200) | 密码哈希 |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(200) | 头像URL |
| bio | VARCHAR(500) | 个人简介 |
| gender | VARCHAR(10) | 性别 |
| birthday | VARCHAR(20) | 生日 |
| location | VARCHAR(100) | 所在地 |
| trust_score | FLOAT | 信任分（0-100） |
| level_id | INTEGER FK→user_levels.id | 用户等级 |
| is_muted | BOOLEAN | 是否禁言 |
| is_admin | BOOLEAN | 是否管理员 |
| created_at | DATETIME | 注册时间 |

### 2. `user_levels` 用户等级表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 等级ID |
| name | VARCHAR(50) | 等级名称 |
| max_free_recipes | INTEGER | 每日发布配方次数（数据库兼容列名） |
| max_works | INTEGER | 每日发布作品次数 |
| max_views | INTEGER | 每日查看配方数 |
| description | VARCHAR(200) | 描述 |
| sort_order | INTEGER | 排序 |

### 3. `user_settings` 用户默认设置

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | INTEGER PK FK→users.id | 用户ID |
| materials | TEXT(JSON) | 默认材料列表 |
| kiln_types | TEXT(JSON) | 默认窑类型 |
| temperatures | TEXT(JSON) | 默认温度 |
| firing_curve_id | INTEGER FK→firing_curves.id | 默认烧制曲线 |
| body_material | VARCHAR(50) | 默认坯体料 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 用户每日额度

`user_usage_quotas` 保存用户当天剩余的配方、作品和查看配方额度；`quota_date` 用于幂等日切，`redeem_count` 为累计兑换次数，不随日切重置。

`user_daily_recipe_views` 按用户、配方和日期做唯一约束，保证同一配方同一天只消耗一次查看额度。

---

## 二、配方系统

### 4. `recipes` 配方表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 配方ID |
| user_id | INTEGER FK→users.id | 发布者 |
| title | VARCHAR(100) | 配方标题 |
| recipe_no | VARCHAR(10) UNIQUE | 编号（A001-Z999） |
| type | VARCHAR(20) | 类型：recipe/firing/matching |
| cover | VARCHAR(200) | 封面图 |
| images | TEXT(JSON) | 多图 |
| describe | TEXT | 配方描述 |
| category | VARCHAR(30) | 分类 |
| temperature | VARCHAR(30) | 温度 |
| atmosphere | VARCHAR(20) | 气氛 |
| kiln_type | VARCHAR(30) | 窑炉类型 |
| kiln_type_other | VARCHAR(50) | 自定义窑类型 |
| body_material | VARCHAR(30) | 坯体料 |
| turnaround | VARCHAR(50) | 周转时间 |
| contact | VARCHAR(200) | 联系方式 |
| visibility | VARCHAR(20) | 可见性：private/public |
| likes | INTEGER | 点赞数 |
| work_count | INTEGER | 关联作品数 |
| created_at | DATETIME | 发布时间 |
| updated_at | DATETIME | 更新时间 |
| forked_from | INTEGER | 二改来源配方ID |
| source | VARCHAR(20) | 外部数据来源标识 |
| source_id | VARCHAR(50) | 来源原始ID |
| glaze_colors | TEXT(JSON) | 釉色 [{hex,r,g,b,name}] |

### 5. `recipe_ingredients` 配方配料表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| recipe_id | INTEGER FK→recipes.id | 配方ID |
| recipe_no | VARCHAR(10) | 配方编号 |
| name | VARCHAR(200) | 原料名 |
| name_en | VARCHAR(200) | 英文名 |
| amount | VARCHAR(100) | 用量 |
| note | TEXT | 备注 |
| is_additional | INTEGER | 是否附加（0=否,1=是） |
| sort_order | INTEGER | 排序 |

### 6. `recipe_views` 配方浏览记录

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 浏览者 |
| recipe_id | INTEGER FK→recipes.id | 配方 |
| created_at | DATETIME | 浏览时间 |

### 7. `recipe_sequences` 配方编号计数器

| 字段 | 类型 | 说明 |
|------|------|------|
| letter | VARCHAR(1) PK | 字母（A-Z） |
| counter | INTEGER | 当前计数 |
| digits | INTEGER | 位数（3→999，4→9999） |

---

## 三、作品系统

### 8. `works` 作品表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 作品ID |
| user_id | INTEGER FK→users.id | 作者 |
| recipe_id | INTEGER FK→recipes.id | 关联配方 |
| image | TEXT | 主图 |
| images | TEXT(JSON) | 多图 |
| description | TEXT | 描述 |
| body_material | VARCHAR(30) | 坯体料 |
| kiln_type | VARCHAR(30) | 窑类型 |
| kiln_type_other | VARCHAR(50) | 自定义窑类型 |
| temperature | VARCHAR(30) | 温度 |
| likes | INTEGER | 点赞数 |
| glaze_colors | TEXT(JSON) | 釉色 [{hex,r,g,b,name}] |
| surface | VARCHAR(20) | 釉面质感 |
| transparency | VARCHAR(20) | 透明度 |
| curve_id | INTEGER FK→firing_curves.id | 关联烧制曲线 |
| created_at | DATETIME | 发布时间 |

### 9. `work_comments` 作品评论表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| parent_id | INTEGER FK self | 父评论（支持嵌套） |
| work_id | INTEGER FK→works.id | 作品 |
| user_id | INTEGER FK→users.id | 评论者 |
| content | TEXT | 内容 |
| created_at | DATETIME | |

### 10. `work_attribute_options` 作品属性配置

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| category | VARCHAR(30) | 属性类：type/body_material/kiln_type/temperature/surface/transparency |
| value | VARCHAR(50) | 可选值 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

---

## 四、评价

### 12. `reviews` 评价表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| parent_id | INTEGER FK self | 父评价（嵌套回复） |
| recipe_id | INTEGER FK→recipes.id | 配方 |
| work_id | INTEGER FK→works.id | 作品 |
| user_id | INTEGER FK→users.id | 评价者 |
| rating | INTEGER | 评分（1-5） |
| content | TEXT | 内容 |
| image | VARCHAR(200) | 图片 |
| body_material | VARCHAR(30) | 坯体料 |
| kiln_type | VARCHAR(30) | 窑类型 |
| kiln_type_other | VARCHAR(50) | 自定义窑类型 |
| temperature | VARCHAR(30) | 烧成温度 |
| created_at | DATETIME | |

---

## 五、社交互动

### 13. `favorites` 收藏表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 用户 |
| recipe_id | INTEGER FK→recipes.id | 收藏的配方 |
| work_id | INTEGER FK→works.id | 收藏的作品 |
| created_at | DATETIME | |

### 14. `likes` 点赞表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 点赞者 |
| recipe_id | INTEGER FK→recipes.id | 配方 |
| work_id | INTEGER FK→works.id | 作品 |
| created_at | DATETIME | |

### 15. `follows` 关注表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| follower_id | INTEGER FK→users.id | 关注者 |
| followed_id | INTEGER FK→users.id | 被关注者 |
| created_at | DATETIME | |

### 16. `messages` 私信表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| sender_id | INTEGER FK→users.id | 发信人 |
| receiver_id | INTEGER FK→users.id | 收信人 |
| content | TEXT | 内容 |
| recipe_id | INTEGER FK→recipes.id | 关联配方 |
| work_id | INTEGER FK→works.id | 关联作品 |
| created_at | DATETIME | |
| is_read | BOOLEAN | 是否已读 |

### 17. `notifications` 通知表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 接收者 |
| from_user_id | INTEGER FK→users.id | 触发者 |
| type | VARCHAR(32) | 类型：comment/follow/like/favorite |
| work_id | INTEGER FK→works.id | 关联作品 |
| content | TEXT | 内容 |
| is_read | BOOLEAN | 是否已读 |
| created_at | DATETIME | |

### 18. `complaints` 投诉/反馈表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 用户 |
| content | TEXT | 内容 |
| images | TEXT | 图片 |
| status | VARCHAR(20) | 状态：open/resolved |
| reply | TEXT | 客服回复 |
| admin_id | INTEGER FK→users.id | 处理管理员 |
| replied_at | DATETIME | 回复时间 |
| created_at | DATETIME | |

---

## 六、材料库存

### 19. `user_materials` 用户材料库存

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 用户 |
| name | VARCHAR(100) | 材料名 |
| category | VARCHAR(50) | 分类 |
| status | VARCHAR(10) | owned（拥有）/ wishlist（想买） |
| from_recipe_id | INTEGER FK→recipes.id | 来源配方 |
| work_id | INTEGER FK→works.id | 来源作品 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

---

## 七、釉料知识

### 20. `materials` 统一原材料目录

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| name | VARCHAR(200) | 中文名 |
| name_en | VARCHAR(200) | 英文名 |
| source | VARCHAR(20) | 数据来源 |
| source_id | INTEGER | 来源数据ID |
| formula | VARCHAR(200) | 分子式 |
| molecular_weight | VARCHAR(50) | 分子量 |
| sio2 等 | FLOAT | 氧化物含量 |
| category | VARCHAR(50) | 分类 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

### 21. `glossary_terms` 陶瓷术语表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| term | VARCHAR(100) | 术语 |
| definition | TEXT | 定义 |
| category | VARCHAR(50) | 分类 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

### 22. `temperature_cones` 奥尔顿测温锥对照表（附录8）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| cone_no | VARCHAR(10) | 温锥号（如09,1,14） |
| temp_60c | INTEGER | 60°C/h 温度 |
| temp_108f | INTEGER | 108°F/h 温度 |
| temp_150c | INTEGER | 150°C/h 温度 |
| temp_270f | INTEGER | 270°F/h 温度 |
| sort_order | INTEGER | |
| created_at | DATETIME | |

### 23. `firing_curves` 烧制曲线表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| name | VARCHAR(100) | 曲线名 |
| type | VARCHAR(20) | 氧化/还原 |
| target_temp | VARCHAR(20) | 目标温度 |
| segments | TEXT(JSON) | 升温段 [{rate,temp,hold}] |
| description | TEXT | 说明 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

### 24. `body_materials` 坯体料类型

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| name | VARCHAR(50) UNIQUE | 名称 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | |

---

## 九、系统配置

### 26. `app_settings` 应用配置键值表

| 字段 | 类型 | 说明 |
|------|------|------|
| key | VARCHAR(100) PK | 配置键 |
| value | TEXT | 配置值（JSON） |
| updated_at | DATETIME | 更新时间 |

预置配置项：
- `verification_settings`：验证码配置（SMTP/短信/调试模式）
- `work_search_temperature_ranges`：作品搜索温度范围
- `work_search_color_ranges`：作品搜索颜色范围

## 十、待烧队列

### 28. `to_be_fired` 待烧表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK→users.id | 用户 |
| recipe_id | INTEGER FK→recipes.id | 配方 |
| note | VARCHAR(200) | 备注 |
| status | VARCHAR(20) | pending/firing/done |
| created_at | DATETIME | |
| updated_at | DATETIME | |

---

## 附表：API 路由总览

### 云窑后台管理（端口 8003）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /admin/users?page=&page_size=&q= | 用户管理列表 |
| GET | /admin/stats | 系统概览统计 |

### 原材料公开接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /materials/catalog?q=&page=&page_size= | 统一原材料目录（无需token） |
| GET | /materials/catalog/{id} | 统一原材料详情 |

### 云窑主 API（端口 8003）

> 详情见 routes/ 目录中各模块

| 路由前缀 | 文件 | 说明 |
|----------|------|------|
| /api/recipes | routes/recipes.py | 配方 CRUD、搜索、筛选 |
| /api/auth | routes/auth.py | 账号登录、验证码 |
| /api/users | routes/users.py | 用户信息、设置 |
| /api/works | routes/works.py | 作品 |
| /api/favorites | recipes.py | 收藏 |
| /api/reviews | recipes.py | 评价 |
| /api/materials | routes/materials.py | 用户材料库与统一原材料目录 |
| /api/curves | routes/curves.py | 烧制曲线 |
| /api/temperature-cones | routes/temperature_cones.py | 测温锥 |
| /api/glossary | routes/glossary.py | 术语表 |
| /api/notifications | routes/notifications.py | 通知 |
| /api/messages | social.py | 私信 |
| /api/complaints | routes/complaints.py | 投诉/反馈 |
| /api/uploads | routes/upload.py | 文件上传 |
| /api/settings | routes/settings.py | 系统设置 |
| /api/recipe-ingredients | routes/recipe_ingredients.py | 配料表 |

*最后更新：2026-07-13*
